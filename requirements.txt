# =============================================================================
# FactorNexus — 核心依赖清单
# Python 3.12+（实测 3.12.10）
#
# 安装：
#   python -m pip install -r requirements.txt
#
# GPU（推荐，RL 引擎加速；无 GPU 自动回退 CPU）：
#   python -m pip install torch --index-url https://download.pytorch.org/whl/cu124
#   （与 requirements 中的 torch>=2.1 版本一致即可）
#
# 本清单由代码 import 扫描生成（2026-08-27），只含真实使用的包。
# 已移除历史遗留：MetaTrader5 / tushare / tvdatafeed / fastapi / uvicorn /
#   python-multipart / pydantic / cryptography / loguru / matplotlib / seaborn /
#   hypothesis / playwright（代码中已无引用，或属可选工具）
# =============================================================================

# ===== 数值计算核心 =====
numpy>=1.26
pandas>=2.1
scipy>=1.11              # 优化器（SLSQP）/ spearmanr / 统计检验
scikit-learn>=1.3        # ML 合成 / 标准化 / stacking meta（Ridge）
pyarrow>=14.0            # parquet 存储（KlineStore / FactorStore）

# ===== 深度学习（RL 引擎 + 策略工厂 MLP/S4 模型）=====
torch>=2.1

# ===== 因子挖掘（GP 引擎多目标进化）=====
pymoo>=0.6               # NSGA-III（model_core/engines/gp_engine.py）

# ===== 策略工厂（M3 基线模型）=====
lightgbm>=4.0            # Qlib 基准模型；缺失时自动回退 sklearn GBDT

# ===== 行情数据源 =====
pytdx>=1.72              # 通达信协议（不复权全历史兜底源）
requests>=2.31           # 腾讯 / 新浪行情 + 东财基本面接口

# ===== 环境与工具 =====
python-dotenv>=1.0       # .env → DEEPSEEK_API_KEY 注入
tqdm>=4.66               # 进度条（挖矿机 / walk-forward）

# ===== 测试 =====
pytest>=8.0

# =============================================================================
# 可选（按需安装，不阻塞主流程）
# =============================================================================
# py-spy        # 进程卡死/性能诊断：pip install py-spy；py-spy dump --pid <PID>
# mamba-ssm     # 真 Mamba（策略工厂 S4 对照的升级件）；Windows 请用纯 Python 版：
#               #   pip install mamba-ssm --no-build-isolation（不含 CUDA 内核）
# matplotlib    # 画图（回测净值/因子曲线等自定义脚本）
